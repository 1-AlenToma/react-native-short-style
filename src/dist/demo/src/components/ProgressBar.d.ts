import * as React from "react";
import { ProgressBarProps } from "../Typse";
export declare const ProgressBar: ({ value, ifTrue, color, speed, css, style, children }: ProgressBarProps) => React.JSX.Element;
